### use fetch API to transform the values into an object and then into an array.
    fetch(url)
      .then(function() {
        // handle the response
      })
      .catch(function() {
        // handle the error
      }); // try to understand this. 
